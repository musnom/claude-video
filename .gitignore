# OS / editor / tooling
.DS_Store
.entire/
__pycache__/
*.pyc
.venv/
.coverage
htmlcov/
.idea/
.vscode/

# build artifact from scripts/build-skill.sh
/dist/

# local Claude Code settings
.claude/settings.local.json

# dev-only audit/planning docs — never committed; a tracked planning doc
# shipped to every plugin install once (exclusion lists drift; .gitignore
# doesn't)
gaps.md
Plan Features.md
